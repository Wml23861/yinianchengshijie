# fuck-it Skill Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a standalone `fuck-it` skill that amplifies a single chapter's dramatic route without changing that chapter's ending target.

**Architecture:** Lock the new contract with focused static text tests first, then update the self-contained `SKILL.md` under `plugins/vibe-noveling/skills/fuck-it/` plus README and design wording. The skill will be shared by `novel-discuss` and `novel-plan`; it must first scan an explicit 15-direction library, then choose 3 same-goal routes whose chapter ending target remains unchanged.

**Tech Stack:** Markdown skill docs, JSON metadata, Python `unittest` static text assertions

---

### Task 1: Add the failing `fuck-it` contract tests

**Files:**
- Modify: `tests/test_novel_write_workflow.py`
- Test: `tests/test_novel_write_workflow.py`

**Step 1: Write the failing test**

Add:

- a `FUCK_IT` path constant
- one test for the new skill contract
- one README contract test for the new mode
- one update to the public-skill frontmatter coverage list

The new assertions should require:

- `name: fuck-it`
- `fuck it` and `fuck-it` trigger language
- support for both `novel-discuss` and `novel-plan`
- a hard rule that the chapter ending target cannot change
- an explicit 15-direction chapter-amplification library
- a promise to scan those 15 directions and then choose 3 same-goal routes
- output fields for conflict escalation, performance amplification, manga-like staging, and same-goal explanation

**Step 2: Run the focused tests to verify they fail**

Run: `python3 -m unittest tests.test_novel_write_workflow.NovelWriteWorkflowTests.test_fuck_it_skill_amplifies_single_chapter_without_changing_end_goal tests.test_novel_write_workflow.NovelWriteWorkflowTests.test_readme_mentions_fuck_it_as_same_goal_chapter_amplifier`

Expected: FAIL because `plugins/vibe-noveling/skills/fuck-it/SKILL.md` does not exist yet and README does not mention the new skill.

### Task 2: Create the `fuck-it` skill and minimal docs

**Files:**
- Create: `plugins/vibe-noveling/skills/fuck-it/SKILL.md`
- Modify: `README.md`
- Modify: `.claude-plugin/marketplace.json`

**Step 1: Reuse the failing tests**

Do not weaken the contract after seeing the failure.

**Step 2: Write minimal implementation**

Create the skill with:

- Chinese instructions centered on the `fuck it` / `fuck-it` trigger
- explicit support for `novel-discuss` and `novel-plan`
- a hard prohibition on changing the chapter ending target
- minimal-context reading instructions that first lock the current chapter endpoint
- a 15-direction library for single-chapter amplification
- a selection step that scans the 15 directions before choosing 3 routes sharing the same endpoint
- mandatory fields for `本章结束目标（固定）`, `冲突升级链`, `角色表现力放大点`, `漫画感 / 场面感装置`, and `为什么终点没变但过程更炸`
- handoff rules for both entry points

Update README and marketplace wording so the public workflow and skill counts stay accurate.

**Step 3: Run the focused tests to verify they pass**

Run: `python3 -m unittest tests.test_novel_write_workflow.NovelWriteWorkflowTests.test_fuck_it_skill_amplifies_single_chapter_without_changing_end_goal tests.test_novel_write_workflow.NovelWriteWorkflowTests.test_readme_mentions_fuck_it_as_same_goal_chapter_amplifier`

Expected: PASS.

### Task 3: Run final regression verification

**Files:**
- Test: `tests/test_novel_write_workflow.py`

**Step 1: Run the full workflow suite**

Run: `python3 -m unittest tests.test_novel_write_workflow`

Expected: PASS with the existing workflow contract plus the new `fuck-it` contract.

**Step 2: Run a targeted content grep**

Run: `rg -n "fuck it|fuck-it|本章结束目标（固定）|为什么终点没变但过程更炸|12 个专业技能" README.md plugins/vibe-noveling/skills/fuck-it/SKILL.md .claude-plugin/marketplace.json`

Expected: matching lines in all updated files.
