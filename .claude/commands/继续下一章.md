---
description: 开发者A：调 xinshang Skill 继续写下一章（不管审没审过，B审不阻塞）
argument-hint: 章号（可空，缺省读47顶部「状态详情」节确定）
---

# /继续下一章 → xinshang Skill 续写

本命令是 **xinshang Skill** 的薄壳（继续下一章）。A 写下一章不强制等 B 审，**A 直接写下一章**。B 审是可选的。

---

## 一、调 Skill 工作流

按 `workflows/continue-next.md` 执行：

1. **加载 Skill** 入口：`.claude/skills/xinshang/SKILL.md`
2. **加载铁律**：`rules/01-deslop.md`（6 Gate） + `rules/02-xinshang.md`（项目铁律 + 跨章不重复） + `rules/03-management.md`（长篇管理）
3. **加载速查**：`references/checklist.md`（写后自检） + `references/script.md`（硬规则脚本） + `references/templates.md`（47 顶部「A 写完状态详情（精简）」模板）
4. **执行流程**：workflows/continue-next.md 七节（触发 / 读资料 / 设计 / 写正文 + 6 Gate / 更新记录 / 回复 / 纪律）

---

## 二、与 /写一章 的区别

| 项 | /写一章 | /继续下一章 |
|----|---------|-------------|
| 读资料 | 全套（含 43/44） | 精简（只必读） |
| 设计 | 12 字段完整 | 12 字段精简 |
| 自检 | 6 Gate + 八轮 + 跨章 + 登场记录 | 6 Gate + 脚本 + 跨章 |
| 滚动复审前两章 | 必做 | 必做 |
| 47 顶部「状态详情」节 | 完整版 | 精简版 |
| B 审 | 不触发，等 /审一章 | 不触发，等 /审一章 |

---

## 三、目标章

读 47 顶部 + 最近章文件 → 推下一章。

- `$ARGUMENTS` 有章号 → 按指定
- 否则 → 47 顶部「进度行 + 状态详情」节确定

---

## 四、项目级必读（精简版）

必读：
- `47_中间人转发话术.md`（顶部进度）
- `39_前100章章节卡.md`（下一章卡）
- `.claude/skills/xinshang/rules/02-xinshang.md`（跨章不重复铁律）

选读（如有疑问）：
- `38_第一卷大纲.md`
- `08_核心人物库.md`
- `16_时间线.md`

不必读：43 / 44 / 审核CLAUDE.md（A 默认掌握；除非 47 提示有新规则变更）。

---

## 五、硬规则脚本

```bash
python novel/工具/硬规则自检.py <章文件>
```

Windows bash 找不到 Python：
```bash
export PATH="/usr/bin:/bin:/f/Program Files (x86)/python:$PATH"; cd "e:/心界/yinianchengshijie" && python novel/工具/硬规则自检.py <章文件>
```

任何 `[X]` 必改到 `[OK]`。**连续句号铁律 `[X]` 即整章不过。**

---

## 六、跨章不重复铁律（必过）

- 现实场景：相邻两章不重样
- 心界人物：不当连续 2 章镜子
- 意象：近 5 章合计 ≤10 次
- 过渡：相邻两章不重样
- 一章「天亮/天黑」最多 2 次

---

## 七、滚动复审前两章（CLAUDE.md 第 82 条附，必做）

- 衔接重复（当前章开头与前章结尾是否逐句重复）
- 状态连续（时间/地点/身体/位置）
- 前文遗留（前两章埋的伏笔/钩子是否遗漏）

---

## 八、更新记录（精简版，**只动三处**）

1. **47 顶部**（一行）：
   ```
   ## 当前：/继续下一章 第 N 章《xxx》已脚本自检 [OK]，等用户决定是否 /审一章
   ```

2. **章节卡 39 追加**：第 N+1 章 8 字段卡

3. **47 顶部「A 第 N 章写完状态详情（精简）」节**（替代创作记录精简版）：
   - 章号 / 标题 / 字数 / 脚本状态
   - 12 字段设计（精简）
   - 修复记录
   - 跨章衔接
   - 章节卡 39 追加 / 数据库更新

**B 审不触发**：等用户敲 `/审一章` 才走 `workflows/review-chapter.md`。

---

## 九、回复模板

```
【第 N 章 写完了 · /继续下一章】
- 标题：xxx
- 字数：xxxx（脚本统计）
- 脚本：[OK]
- 现实 / 心界 配比：x% / y%
- 47 顶部「A 第 N 章写完状态详情（精简）」节：已登记
- 47 顶部：已改为「/继续下一章 第 N 章《xxx》已脚本自检 [OK]，等用户决定是否 /审一章」
→ 敲 /审一章 走 B 审核；不敲可继续 /继续下一章。
```

---

## 十、纪律

- 零破折号、零英文、字数 3000-3300（≥2800 兜底）
- 金句 0-1 句、每章不顿悟
- 写完必跑脚本 + 跨章不重复
- 不自主 git 提交
- 不擅改核心设定
- 不写骗检测器的糙文
- 不奉承、直接改问题
