---
description: 开发者A：调 xinshang Skill 写下一章（新写，或按B提示词改）；不等B审
argument-hint: 章号（可空，缺省读47顶部「状态详情」节确定）
---

# /写一章 → xinshang Skill 写一章

本命令是 **xinshang Skill** 的薄壳（写一章）。A 写正文（**新写，或按 B 的提示词修改**），B 审是**可选**的，不阻塞写下一章。

---

## 一、调 Skill 工作流

按 `workflows/write-chapter.md` 执行：

1. **加载 Skill** 入口：`.claude/skills/xinshang/SKILL.md`
2. **加载铁律**：
   - `rules/01-deslop.md`（6 Gate + 禁用词 + 7 模式）
   - `rules/02-xinshang.md`（项目铁律）
   - `rules/03-management.md`（长篇管理：章节卡/世界线/伏笔/上下文/人物）
3. **加载速查**：`references/checklist.md`（写前+写后自检清单）、`references/templates.md`（章节卡/47 顶部「状态详情」模板）、`references/script.md`（硬规则自检脚本使用指南）
4. **执行流程**：workflows/write-chapter.md 八节（触发 / 读资料 / 设计 / 写正文 / 自查 / 更新记录 / 回复 / 纪律）

---

## 二、目标章

- `$ARGUMENTS` 有章号 → 按指定
- 否则 → 读 47 顶部「进度行 + 状态详情」节确定

---

## 三、项目级必读（Skill 之外）

- `CLAUDE.md`（创作宪法，48 卷，已自动加载）
- `novel/设定库/写作流程/43_人类化写作规则.md`（69 条）
- `novel/设定库/写作流程/44_检测器视角防AI写作规律.md`（11 条）
- 设定库 5 件套：章节卡 `39_前100章章节卡.md` / 大纲 `38_第一卷大纲.md` / 人物 `08_核心人物库.md` + `36_心界众生谱.md` / 伏笔 `13_长期伏笔库.md` / 时间线 `16_时间线.md`
- 前两章正文（必读）

---

## 四、章节卡追加（第 51 章起强制）

写完后 **必须** 在 `39_前100章章节卡.md` 对应幕末尾追加本章的 8 字段卡：
- 第 1-50 章已填齐 8 字段卡
- 第 51 章起空白，写一张填一张
- 写六章就是六章全写完再追加六张，**不一次设计多张**

模板见 `references/templates.md` 第一节。

---

## 五、硬规则脚本

```bash
python novel/工具/硬规则自检.py <章文件>
```

Windows bash 找不到 Python（已知 PATH 问题）：
```bash
export PATH="/usr/bin:/bin:/f/Program Files (x86)/python:$PATH"; cd "e:/心界/yinianchengshijie" && python novel/工具/硬规则自检.py <章文件>
```

任何 `[X]` 必改到 `[OK]`。**连续句号铁律 `[X]` 即整章不过，其它项不评分。**

---

## 六、回复模板

```
【第 N 章 写完了】
- 标题：xxx
- 字数：xxxx（脚本统计）
- 脚本：[OK]（修复记录：…）
- 现实 / 心界 配比：x% / y%
- 章节卡：39 追加第 N+1 张
- 47 顶部：已改为「正等 B 审第 N 章」
- 47 顶部「A 第 N 章写完状态详情」节：已登记
→ B 审不强制，敲 /审一章 走审核。
```

【改】情况：
```
【第 N 章 改完了 · 第 X 轮】
- 改前脚本：[X] N 项 → 改后脚本：[OK]
- P0/P1 逐条：[原问题] → [改法] → [改后第 N 行]
- 47 顶部：已改为「正等 B 复审第 N 章」
→ 复审不强制，敲 /审一章 走流程。
```

---

## 七、纪律

- 零破折号、零英文、字数 3000-3300（≥2800 兜底）
- 金句 0-1 句、每章不顿悟
- 写完必跑脚本 + 八轮自查
- 不自主 git 提交（用户说「提交」才 commit/push）
- 不擅改核心设定（不确定记待确认）
- 不写骗检测器的糙文（CLAUDE.md 第三十七卷禁止）
- 不替用户/人物/作者悟
- 不奉承、直接改问题
