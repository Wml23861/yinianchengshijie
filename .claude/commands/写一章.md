---
description: 开发者A：读46+47→写下一章（新写，或按B提示词改）→跑脚本自检→登记→报告；不等B审
argument-hint: 章号（可空，缺省读47/创作记录确定）
---

你现在是开发者A，只写正文（新写，或按 B 的提示词修改），绝不自己终审。B 审是**可选**的，不是写下一章的前置闸门：本命令不管 B 审没审过、通没通过，都继续写。

先读：CLAUDE.md（创作宪法）、novel/设定库/写作流程/46_开发者A身份与流程.md、43_人类化写作规则.md、47_中间人转发话术.md（含 B 最新修改提示词）、创作记录/最新一条。

确定目标章：$ARGUMENTS 有章号按指定章；否则读 47 顶部状态 + 创作记录/最新一条确定。

分两种情况执行：

【改】若 47 里有 B 的修改提示词（正等 A 改第 N 章）：逐条执行提示词（P1 全改，P3 逐条判断）。改完必须跑脚本自检：
  python novel/工具/硬规则自检.py <章文件>
若报 `python: command not found`（本机 bash 的 PATH 是 Windows 分号格式，已知环境问题），改用：
  export PATH="/usr/bin:/bin:/f/Program Files (x86)/python:$PATH"; cd "e:/心界/yinianchengshijie" && python novel/工具/硬规则自检.py <章文件>
任何 [X] 必须修到 [OK]。再跑八轮自查（尤其引号成对、破折号、字数）。登记 创作记录/，更新 47 顶部为「正等 B 复审第 N 章」，回复「改完了」。

【写】否则（不管 B 审没审过，写下一章）：读章节卡39 + 第一卷大纲38 + 人物08/09 + 伏笔13 + 时间线16 + 前两章正文，第一遍就写足 2800-3300 字（一章一个事件单元，遵守 43 全部铁律）。写完必须跑脚本自检，[X] 全修掉。自查（八轮+43维度），登记 创作记录/，更新 47 顶部为「正等 B 审第 N 章」，回复「写完了」。

铁律：零破折号、零英文、字数≥2800、金句0-1句、每章不顿悟、写完必跑脚本、不自主 git 提交、不擅改核心设定（不确定记待确认）。
