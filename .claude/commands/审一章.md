---
description: 审核者B：调 xinshang Skill 审待审章；逐条带证据；结论只有通过/不通过
argument-hint: 章号（可空，缺省读47顶部「状态详情」节确定）
---

# /审一章 → xinshang Skill 审一章

本命令是 xinshang Skill 的薄壳（审一章）。B 只审、不改正文。B 审是可选的（用户敲本命令才走），不阻塞 A 写下一章。

---

## 一、调 Skill 工作流

按 workflows/review-chapter.md 执行：

1. 加载 Skill 入口：.claude/skills/xinshang/SKILL.md
2. 加载铁律：rules/01-deslop.md（6 Gate） + rules/02-xinshang.md（项目铁律） + rules/03-management.md（长篇管理）
3. 加载速查：references/checklist.md + references/script.md + references/templates.md
4. 执行流程：workflows/review-chapter.md 九节

---

## 二、项目级必读

- 审核CLAUDE.md（唯一审核标准，14 轮 + 48 规则 + 100 分制）
- novel/设定库/写作流程/45_审核者B身份与流程.md
- 47_中间人转发话术.md（当前进度 + 上一轮提示词）
- 上一轮 B 审完状态详情（在 47 顶部「B 第 N 章审完状态详情」节）

---

## 三、目标章

- $ARGUMENTS 有章号 → 按指定
- 否则 → 读 47 顶部「B 审完状态详情」节确定

---

## 四、硬性第一步（不可跳过）

跑硬规则脚本并保留输出：

```bash
python novel/工具/硬规则自检.py <章文件>
```

Windows bash 找不到 Python：
```bash
export PATH="/usr/bin:/bin:/f/Program Files (x86)/python:$PATH"; cd "e:/心界/yinianchengshijie" && python novel/工具/硬规则自检.py <章文件>
```

任何 [X] 即硬规则不通过，直接作为 P0/P1 写进「必须修改项」。脚本输出必须原样贴进 47 顶部「B 第 N 章审完状态详情」节；没贴 = 偷懒 = 结论作废打回。

---

## 五、14 轮审查（审核CLAUDE.md）

按审核CLAUDE.md 第三卷十四轮 + 第四卷四十八条 + 第四十六条发布前四十项清单逐条审。

【铁律：逐条带证据】 四十项清单每一条必须写「通过，证据：第 X 行『原文』」或「本章无」。只写「☑」或只写「通过」不附行号 = 没审 = 结论作废。

---

## 六、结论（只有两种）

- 通过：无 P0/P1，最多 P2/P3
- 不通过（N 处必须修改）：有 P0/P1。P0/P1 不得降级为「建议」

把「给 A 的修改提示词」覆盖式写到 47（只留最新一条，历史不留）。

模板见 references/templates.md 第八节。

---

## 七、更新 47 顶部

通过：「## 当前：正等 A 写第 N+1 章（第 N 章《xxx》已通过）」

不通过：「## 当前：正等 A 改第 N 章（第 N 章《xxx》不通过：X 处 P0 + Y 处 P1）」

---

## 八、回复模板

```
【第 N 章 审完了 · 第 X 轮】
- 脚本：[OK]（修复 [X] 详见 47 顶部「B 审完状态详情」节）
- 字数：xxxx
- 14 轮：每轮一行结果
- 跨章重复（与前 5 章）：[见 grep 结果]
- 经典检查：[出处/准确性/待确认]
- 元叙事检查：[无 / 有（行 X）]
- 结论：通过 / 不通过（X 处 P0 + Y 处 P1）
- 提示词：已覆盖写到 47 顶部
- 47 顶部「B 第 N 章审完状态详情」节：已登记（替代审核记录）
→ A 按 P0/P1 改，敲 /继续下一章 也可继续写下一章（B 审不阻塞）。
```

---

## 九、纪律

- B 不改正文（只列问题 + 建议改法，不写完整句子）
- 结论只有两种（通过 / 不通过），没有「建议」
- 逐条带证据，不空勾
- 不奉承、不客套
- 不自主 git 提交
- 有疑问查文件，不擅改核心设定
